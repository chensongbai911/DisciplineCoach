# 微信开发者工具操作指南 - DisciplineCoach

## 目录

1. [初始化项目](#初始化项目)
2. [云开发环境配置](#云开发环境配置)
3. [部署云函数](#部署云函数)
4. [创建数据库](#创建数据库)
5. [测试云函数](#测试云函数)
6. [前端集成测试](#前端集成测试)
7. [常见问题](#常见问题)

---

## 初始化项目

### 1.1 打开项目

1. **启动微信开发者工具**
2. **点击「打开」或「导入项目」**
3. **选择项目路径：** `d:\DisciplineCoach`
4. **输入 AppID：** `wxd7b17df348c02834`
5. **不使用 npm（如果提示）**
6. **点击「确定」**

### 1.2 验证项目结构

打开后应该看到：

```
左侧文件树：
├── cloudfunctions/
│   ├── user/
│   ├── plan/
│   ├── record/
│   ├── statistics/
│   ├── payment/
│   ├── feedback/
│   └── message/
├── miniprogram/
│   ├── pages/
│   ├── utils/
│   ├── app.js
│   ├── app.json
│   └── app.wxss
├── project.config.json
└── ...
```

### 1.3 检查项目配置

1. **点击菜单：** 工具 → 项目设置
2. **验证基本信息：**
   - AppID: `wxd7b17df348c02834` ✓
   - 项目名称: `DisciplineCoach` ✓
3. **点击「云开发」选项卡**
4. **验证云开发配置：**
   - 环境ID: `cloud1-0g29mlsv3d4ca637` ✓

### 1.4 编译小程序

1. **菜单：** 工具 → 编译
2. **或按快捷键：** Ctrl + B
3. **等待编译完成**
4. **右侧预览窗口应该显示小程序首页**

---

## 云开发环境配置

### 2.1 打开云开发控制台

1. **点击菜单栏下方的「云开发」按钮**
   - 如果看不到，可能是项目配置问题
   - 重新检查 AppID 和云环境 ID

2. **进入云开发控制台**
   - 应该看到环境信息：`cloud1`
   - 左侧菜单：数据库、云函数、存储等

### 2.2 验证环境配置

在云开发控制台首页检查：

```
环境信息：
- 环境名称: cloud1
- 环境ID: cloud1-0g29mlsv3d4ca637
- 配额使用情况
- 存储空间
```

**如果显示错误或找不到环境：**
1. 检查 `project.config.json` 中的 envId
2. 确保云环境在微信后台已创建
3. 重新启动微信开发者工具

---

## 部署云函数

### 3.1 同步云函数列表

1. **在左侧文件树中右键点击 `cloudfunctions` 文件夹**
2. **选择「同步云函数列表」**
3. **等待同步完成**
   - 应该显示 7 个云函数
   - 可能显示为「本地」状态

### 3.2 逐个部署云函数

#### 步骤1：部署 user 云函数

1. **右键点击 `cloudfunctions/user` 文件夹**
2. **选择「上传并部署：云端安装依赖（不上传 node_modules）」**
3. **确认对话框中的设置**
4. **等待上传完成**
   - Console 中显示：`函数 user 上传完毕`
   - 状态变为「已部署」

#### 步骤2：重复其他云函数

对以下文件夹重复上述步骤：

```
plan
  ↓
record
  ↓
statistics
  ↓
payment
  ↓
feedback
  ↓
message
```

**部署顺序的原因：**
- `user` 是基础（登录和用户信息）
- `plan` 是核心功能（计划数据）
- `record` 依赖 `plan`（打卡记录）
- 其他函数相对独立

### 3.3 验证部署状态

1. **进入云开发控制台**
2. **点击「云函数」**
3. **检查列表：**

```
✓ user     已部署
✓ plan     已部署
✓ record   已部署
✓ statistics 已部署
✓ payment  已部署
✓ feedback 已部署
✓ message  已部署
```

**如果有函数显示「未部署」或出错：**
1. 点击该函数，查看「详情」和「日志」
2. 检查是否有 package.json 依赖错误
3. 重新部署该函数

---

## 创建数据库

### 4.1 打开数据库管理

1. **在云开发控制台左侧菜单点击「数据库」**
2. **选择「集合」选项卡**

### 4.2 创建集合：users

1. **点击「新建集合」按钮**
2. **输入集合名称：** `users`
3. **点击「确定」创建**

#### 创建索引：_openid

1. **在 users 集合中，点击「索引」选项卡**
2. **点击「新增索引」**
3. **配置索引：**
   - 字段名：`_openid`
   - 索引类型：`唯一索引`
4. **点击「确定」**

**预期结果：**
```
索引列表：
- _id (系统自动，唯一)
- _openid (唯一)
```

### 4.3 创建集合：plans

1. **点击「新建集合」，输入名称：** `plans`
2. **创建完成后，点击「索引」选项卡**

#### 创建索引 1：_openid

```
字段名：_openid
索引类型：单字段索引
```

#### 创建索引 2：status

```
字段名：status
索引类型：单字段索引
```

#### 创建索引 3：_openid + status（复合）

```
字段1：_openid
字段2：status
索引类型：复合索引
```

### 4.4 创建集合：records

#### 索引配置

```
1. _openid (单字段)
2. date (单字段)
3. planId (单字段)
4. _openid + date (复合)
```

**操作同上，重复创建索引**

### 4.5 创建集合：orders

#### 索引配置

```
1. _openid (单字段)
2. orderNo (唯一索引)
3. status (单字段)
```

### 4.6 创建集合：feedbacks

#### 索引配置

```
1. _openid (单字段)
2. status (单字段)
3. createTime (单字段)
```

### 4.7 验证所有集合

在数据库管理页面应该看到：

```
集合列表：
- feedbacks
- orders
- plans
- records
- users

(按字母顺序排列)
```

---

## 测试云函数

### 5.1 在云开发控制台测试

#### 5.1.1 测试 user.login

1. **打开云开发控制台**
2. **点击「云函数」**
3. **点击 `user` 函数**
4. **右侧面板，点击「测试」按钮**
5. **在「输入」框粘贴以下内容：**

```json
{
  "action": "login"
}
```

6. **点击「运行」按钮**
7. **查看返回结果：**

```json
{
  "success": true,
  "data": {
    "_id": "xxx...",
    "nickName": "WeChat User",
    "isVip": false,
    "isNewUser": true
  }
}
```

#### 5.1.2 测试 plan.create（创建测试计划）

1. **点击 `plan` 函数**
2. **点击「测试」**
3. **输入以下内容：**

```json
{
  "action": "create",
  "params": {
    "name": "每日运动",
    "category": "health",
    "target": 10,
    "unit": "km",
    "frequency": "daily"
  }
}
```

4. **点击「运行」**
5. **预期返回：**

```json
{
  "success": true,
  "data": {
    "_id": "plan_id_xxx",
    "name": "每日运动",
    "createdAt": 1705276800000
  }
}
```

**记下这个 planId，后面的测试会用到**

#### 5.1.3 测试 record.create（创建打卡记录）

1. **点击 `record` 函数**
2. **点击「测试」**
3. **输入以下内容（将 planId 替换为上面返回的 ID）：**

```json
{
  "action": "create",
  "params": {
    "planId": "替换为上面获得的plan_id_xxx",
    "completion": 8.5,
    "note": "完成了大部分"
  }
}
```

4. **点击「运行」**
5. **预期返回成功**

#### 5.1.4 其他云函数测试

参考 `CLOUD_FUNCTION_QUICK_TEST.md` 中的详细用例。

### 5.2 在小程序中测试

#### 5.2.1 启动小程序调试

1. **点击「编译」按钮（Ctrl + B）**
2. **右侧预览窗口显示小程序**
3. **打开「调试」工具（菜单 → 调试 → 开启调试）**

#### 5.2.2 查看 Console 日志

1. **在预览窗口点击「Console」选项卡**
2. **执行以下命令测试云函数：**

```javascript
// 登录测试
wx.cloud.callFunction({
  name: 'user',
  data: { action: 'login' }
}).then(res => console.log('登录:', res.result))
```

3. **按 Enter 执行**
4. **查看返回结果**

---

## 前端集成测试

### 6.1 首页加载测试

#### 操作步骤

1. **启动小程序**
   - 在预览窗口中应该看到首页

2. **打开开发者工具 Console**
   - 菜单 → 调试 → Console

3. **观察日志输出**
   ```
   [app.js] 检查登录状态
   [app.js] 登录成功
   [index.js] 加载数据中...
   [index.js] 加载计划列表
   [index.js] 加载今日记录
   [index.js] 处理数据完成
   ```

4. **验证页面显示**
   ```
   ✓ 今日日期
   ✓ 小教练消息
   ✓ 统计数据（完成/总数，完成度%，连续天数）
   ✓ 计划列表（如果有计划的话）
   ```

### 6.2 打卡流程测试

#### 前提条件

需要在云开发控制台创建至少一个测试计划（使用 plan.create 测试）

#### 操作步骤

1. **在首页找到一个计划/任务**
2. **点击该任务**
3. **验证弹窗：**
   ```
   ✓ 弹窗显示计划名称
   ✓ 输入框显示（或为空，如果未打卡）
   ✓ 备注框显示（或为空）
   ✓ 确认和取消按钮
   ```

4. **输入完成值**
   ```
   例如：8.5（对于"每日运动 10km"）
   ```

5. **输入备注（可选）**
   ```
   例如：跑步了8.5km
   ```

6. **点击「确认打卡」按钮**
7. **验证结果：**
   ```
   ✓ 弹窗关闭
   ✓ 显示成功提示："打卡成功！"
   ✓ 页面刷新
   ✓ 该任务显示已打卡的数值
   ✓ 完成度更新
   ✓ 进度条更新
   ```

### 6.3 编辑打卡记录

#### 操作步骤

1. **点击已打卡的任务**
2. **验证弹窗中显示之前的完成值**
3. **修改完成值**
4. **点击确认**
5. **验证页面更新为新的数值**

---

## 常见问题

### Q1: 云开发按钮消失或无法打开云开发控制台

**原因：**
- AppID 不正确
- 项目配置缺少 envId

**解决方案：**
1. 检查 `project.config.json`：
   ```json
   {
     "appid": "wxd7b17df348c02834",
     "envId": "cloud1-0g29mlsv3d4ca637",
     "cloudfunctionRoot": "cloudfunctions/"
   }
   ```
2. 重新启动微信开发者工具

### Q2: 云函数部署失败，错误信息："Cannot find module 'wx-server-sdk'"

**原因：**
- 云函数没有正确上传依赖

**解决方案：**
1. 确保选择「上传并部署：云端安装依赖」选项
2. 检查 `cloudfunctions/xxx/package.json` 是否包含 wx-server-sdk 依赖
3. 删除该函数（在云开发控制台），重新部署

### Q3: 测试云函数时返回 "Database permission denied"

**原因：**
- 集合权限设置不当
- 或集合不存在

**解决方案：**
1. 检查所有 5 个集合是否都已创建
2. 检查集合权限是否为：`read: false, write: false`
3. 这是正确的设置，所有操作必须通过云函数进行

### Q4: 小程序首页显示空白或错误

**原因：**
- 云函数调用失败
- 前端代码错误
- 网络连接问题

**解决方案：**
1. 打开 Console，查看错误信息
2. 检查云函数是否已部署且状态正常
3. 尝试在云开发控制台直接测试云函数
4. 检查小程序网络调试信息

### Q5: 打卡后页面没有刷新

**原因：**
- 云函数执行失败
- 前端没有正确处理返回值

**解决方案：**
1. 在 Console 中查看 record.create 返回值
2. 检查云函数日志（云开发控制台 > 云函数 > 日志）
3. 在提交打卡时，添加错误处理代码

### Q6: 如何查看云函数日志

**步骤：**
1. 进入云开发控制台
2. 点击「云函数」
3. 点击要查看的函数名
4. 点击「日志」选项卡
5. 查看最近的执行日志

**日志中通常包含：**
```
{
  "RequestId": "xxx...",
  "StartTime": 1705276800000,
  "Duration": 123,
  "MemUsage": 64,
  "ReturnValue": { ... },
  "InvokeResult": 0 (0表示成功，其他值表示失败)
}
```

### Q7: 如何清空数据库进行重新测试

**步骤：**
1. 在云开发控制台点击「数据库」
2. 选择要清空的集合（如 plans）
3. 全选所有记录（使用右上角复选框）
4. 点击「删除」确认

**警告：** 这将永久删除数据，无法恢复。建议只在测试时使用。

---

## 快速查询表

### 常用快捷键

| 功能 | 快捷键 |
|------|--------|
| 编译 | Ctrl + B |
| 预览 | Ctrl + P |
| 打开调试工具 | F12 |
| 刷新 | F5 |
| 清空缓存 | Ctrl + L |

### 云开发常用链接

在云开发控制台的快速链接：
- 环境信息：显示环境 ID、存储配额等
- 数据库：集合管理、数据查看
- 云函数：函数管理、部署、测试、日志
- 存储：文件管理、上传下载
- 调用统计：函数调用次数、成本统计

---

## 下一步

完成以上所有测试后：

1. ✅ 所有 7 个云函数已部署
2. ✅ 所有 5 个数据库集合已创建
3. ✅ 云函数测试通过
4. ✅ 首页前端集成成功
5. ✅ 打卡流程正常工作

**接下来的工作：**
- 完成其他页面的开发和集成
- 测试完整的用户流程
- 优化性能和用户体验

---

**文档版本：** 1.0
**最后更新：** 2024年
**问题反馈：** 请查看 PHASE1_INTEGRATION.md 的常见问题排查部分
