# 「自律教练」微信小程序需求文档（V1.0）

> 前端技术栈：**原生微信小程序**（WXML + WXSS + JS + 原生组件）  
> 后端：建议使用微信云开发（CloudBase），如后面有更复杂需求可迁移到自建服务器

---

## 一、产品概述

### 1.1 产品名称（暂定）

- 中文名：自律教练 / 习惯监督官（可后续再定）
- 英文名（选填）：Self Coach

### 1.2 产品定位

一个专注于日常习惯养成与个人成长的微信小程序，帮助用户在「运动、饮食、睡眠、阅读、学习」五个核心维度建立计划、坚持打卡，并通过数据反馈与拟人化小教练角色，提供持续、友好、生动的监督和激励。

### 1.3 核心目标

- 让用户清晰地制定每日计划  
- 通过简单、愉快的打卡流程，让用户减少拖延  
- 通过连续天数、统计数据、激励反馈，增强成就感  
- 通过会员制提供更强的功能和激励工具，实现产品变现

### 1.4 目标用户

- 年龄：20–40 岁
- 特征：有改善自己生活习惯的意愿，自控力一般，希望被温柔监督
- 典型用户：
  - 上班族：想减脂、保持良好作息与学习
  - 学生：要备考、想保持规律学习和作息
  - 家庭主妇/宝妈：希望兼顾健康、学习和家庭

---

## 二、整体功能架构

### 2.1 功能模块概览

1. 用户与账户模块
2. 计划管理模块
3. 打卡与记录模块
4. 数据统计与可视化模块
5. 会员付费与权益模块
6. 激励与监督机制模块
7. 设置与其他模块（反馈、关于等）

### 2.2 页面架构（V1 建议）

1. 启动/引导页（可选）
2. 首页 / 今日看板页
3. 计划设置页（整体入口）
   - 运动计划设置
   - 饮食计划设置
   - 睡眠计划设置
   - 阅读计划设置
   - 学习计划设置
4. 打卡记录详情页（按单日或单维度）
5. 数据统计页（周/月统计）
6. 会员中心页
7. 个人中心 / 设置页

---

## 三、详细功能需求

### 3.1 用户与账户模块

#### 3.1.1 登录与身份标识

- 使用微信小程序的登录能力：
  - 使用 `wx.login` 获取 `code`
  - 后端通过 `code` 换取 `openid`，作为用户唯一标识
- 新用户首次进入：
  - 弹出授权头像昵称的引导（非强制，但推荐）
  - 若用户拒绝，仅以匿名方式使用，但仍保留 openid 作为主键

**数据字段（示例）：**

- `user_id`（等于 openid）
- `nickname`
- `avatar_url`
- `gender`
- `created_at`
- `last_login_at`
- `is_member`（布尔）
- `member_expire_at`

#### 3.1.2 用户首次引导流程

- 第一次使用时引导：
  1. 欢迎页 + 简要说明（本产品可以帮你监督日常五个方面）
  2. 引导用户勾选自己最关心的维度（默认全部勾选，可取消）
  3. 简单默认计划模板（例如运动 30 分钟/天 等），用户可稍后修改

---

### 3.2 计划管理模块

#### 3.2.1 计划维度

默认五大维度：

1. 运动
2. 饮食
3. 睡眠
4. 阅读
5. 学习

**需求：**

- 在计划设置总页面展示这 5 个维度，每个可以单独进入配置页
- 每个维度都可以**开启/关闭**
  - 关闭后，该维度不显示在首页「今日任务」列表中
  - 数据不删除，可随时重新开启

#### 3.2.2 各维度计划配置需求

##### A. 运动计划

字段设计（可在一个维度中支持多条计划项，但 V1 可限制为 1–3 条）：

- 计划名称（可编辑，默认“运动”）
- 目标类型（单选）：
  1. 按时长：单位分钟（如 30 分钟）
  2. 按步数：整数（如 8000 步）（可预留接口对接微信步数）
  3. 按次数/次数型任务：整数（如 完成 1 次力量训练）
- 执行频率：
  - 每周重复：选择周一到周日（多选）
  - 默认周一～周日全选
- 提醒设置（V1 可简单实现）：
  - 是否开启提醒（布尔）
  - 提醒时间（例如 20:00）
  - 采用微信订阅消息机制（由用户主动授权后发送）

##### B. 饮食计划

- 计划类型：勾选式习惯列表：
  - 吃早餐（是/否）
  - 不喝含糖饮料（是/否）
  - 不吃宵夜（是/否）
  - 每天喝水 ≥ X 杯（数字，可配置）
- 用户可自定义新的饮食习惯条目（V1 可选做一个，限制最多 3 条）
- 执行频率：同上（每周若干天）
- 提醒设置：可分早/中/晚三个时间段（V1 可以只选一个提示时间）

##### C. 睡眠计划

- 指标设置：
  - 入睡截止时间：比如 23:30 前
  - 睡眠时长：例如 ≥ 7 小时
- 记录方式：
  - 用户打卡时：
    - 输入睡觉时间（或选择时间组件）
    - 输入起床时间（或选择时间组件）
  - 系统自动计算睡眠时长，判断是否达标
- 提醒：
  - 睡前提醒时间（如 23:00）

##### D. 阅读计划

- 目标形式：
  - 按时长：如 阅读 20 分钟
  - 按页数：如 阅读 10 页
- 书籍记录：
  - 当前阅读书名（文本）
  - 可选：当前阅读进度（页数或章节）（V1 可简化为备注字段）
- 执行频率：同上（每周某几天）

##### E. 学习计划

- 目标形式：
  - 按时长：如 学习 30 分钟
  - 按任务：如 完成 1 节课程（V1 可采用简单描述文本+完成与否）
- 标签（可选）：
  - 如：英语、编程、考证，用户可选择已有或新增一个
- 执行频率：同上（每周某几天）

---

### 3.3 打卡与记录模块

#### 3.3.1 今日任务列表（首页核心）

- 首页展示「今日任务列表」，按维度分组或混合展示：

示例布局：

- 顶部：今日日期 + 小教练一句话（如“今天也要元气满满哦！”）
- 中部：今日任务卡片列表：
  - 每个维度为一个可展开卡片，显示：
    - 图标（运动/饮食/睡眠/阅读/学习）
    - 该维度今日任务完成进度（例如 1/2、已完成/未完成）
    - 展开后展示具体任务项 + 打卡按钮
- 底部：今日完成概览（完成X/Y 个任务） + 今日打卡进度条

#### 3.3.2 打卡交互设计

**基础要求：**

- 每个任务项有明显的「打卡」按钮
- 点击打卡：
  - 若是简单任务（如“完成一次运动 30 分钟”）：  
    - 直接弹出确认框或小弹窗，供用户填写实际完成情况（如真实分钟数/备注），或直接“完成”
  - 若是时间类或量化任务：
    - 弹出输入框（数字+单位），可以有默认值（比如目标值）
- 完成后：
  - 显示轻量反馈动画（如小教练表情+短句）
  - 更新今日任务列表状态
  - 更新顶部“已完成任务数”

**补打卡机制：**

- V1 允许补打卡的范围：
  - 仅允许对**过去 7 天**内的日期进行补打卡
  - 默认用户可自由补打卡（若要做限制，可放入会员功能）
- 补打卡入口：
  - 在日历视图或统计页，可点某一天日期进入“当日记录详情”，进行补打卡或修改

#### 3.3.3 连续打卡计算规则

- 对每个维度，记录连续完成的天数：
  - 某天该维度下所有计划项全部达标，视为当日该维度完成
- 当天未完成则连续天数归零（如后续要支持“请假日/补签卡”再做调整）
- 首页显示：
  - 总连续打卡天数（综合）
  - 各维度连续天数（可展示在数据页）

---

### 3.4 数据统计与可视化模块

#### 3.4.1 周 / 月视图

- 入口：底部 Tab 或首页链接“查看数据”
- 维度选择：
  - 先显示综合统计，再可以切换到单个维度的统计

**综合统计：**

- 统计项：
  - 最近 7 天/30 天整体打卡完成率（例如：80%）
  - 平均每天完成的任务数量
  - 各维度达标天数柱状图/条形图

**单维度统计（例如运动）：**

- 最近 7 天/30 天的完成情况折线图/柱状图
- 连续最多完成天数记录
- 平均值（如 平均运动时长、平均睡眠时长）

#### 3.4.2 数据展示形式（V1 可简单，后续美化）

- 使用微信小程序 Canvas 或第三方图表库（如 ECharts for Weixin）
- 图表类型：
  - 柱状图（完成天数）
  - 折线图（完成率/时间趋势）

#### 3.4.3 日报 / 周报 / 月报（V1 简化）

- V1 先做一个简单的「周总结」页面，用户手动查看即可
- 总结内容示例：
  - “本周你坚持打卡 5 天，其中运动完成 4 天，睡眠连续 3 天达标。”
  - 生成一段可分享的文字摘要

---

### 3.5 会员付费与权益模块

#### 3.5.1 收费方案（V1 建议）

- 订阅制会员：
  - 月度会员：18 元/月
  - 季度会员：48 元/3 个月
  - 年度会员：168 元/年
- 新人优惠：
  - 新用户首次开通可享受首月 9.9 元（选做）

#### 3.5.2 会员购买流程

- 入口：
  - 首页顶部“开通会员”banner
  - 个人中心/会员中心入口
- 购买步骤：
  1. 选择套餐（按月/季/年）
  2. 确认协议（服务协议、自动续费说明如有）
  3. 调用微信支付（小程序支付）
  4. 支付成功后，更新用户 `is_member` 和 `member_expire_at` 字段

#### 3.5.3 会员特权（V1）

**免费用户：**

- 至多x条计划（例如每个维度仅支持 1 条计划）
- 简单周数据统计
- 无补签卡/请假日
- 无高级激励工具

**会员用户：**

1. **计划能力增强**
   - 每个维度可创建更多计划条目（如 3–5 条）
   - 自定义维度（新增例如：冥想、写日记、理财等习惯模块）——V1 可做一个“自定义习惯”简单版（不区分五大类，仅作为附加列表）

2. **数据与报告增强**
   - 查看过去 90 天数据
   - 提供更详细的图表（如平均值、最大值）
   - 周/月总结文字报告

3. **激励工具**
   - 每月赠送 X 张补签卡（如 3 张）
   - 设置“休息日/请假日”（每月 1–2 天），休息日不打断连续天数
   - 特殊徽章和小教练皮肤解锁（V1 可先留一个简单皮肤切换）

4. **外观与个性化**
   - 可切换主题色
   - 小教练更多语气包（可预留）

#### 3.5.4 会员状态与到期提醒

- 在个人中心、首页明显位置显示：
  - 会员类型（普通/会员）
  - 到期日期
- 到期前 3 天通过订阅消息提醒（需用户授权订阅）：
  - 内容示例：“你的会员将在 3 天后到期，本月你已经坚持打卡 20 天，继续保持吗？”

---

### 3.6 激励与监督机制模块

#### 3.6.1 小教练角色

- 形象：简单卡通头像（前端先用占位图，后续设计可替换）
- 展现位置：
  - 首页顶部
  - 打卡完成时弹窗中
  - 统计页摘要位置

**文案规则：**

- 根据用户当日完成情况动态显示：
  - 全部完成：积极赞扬 + 强调成就
  - 部分完成：鼓励继续 + 提醒剩余任务数量
  - 完全未完成（晚上）：安慰 + 建议先从小目标开始

（V1 可以先写死 3–5 条文案，按情况随机选一条）

#### 3.6.2 等级与经验值（可选做简化版）

- 每完成一个任务获得一定经验值（如 +10）
- 经验累计到一定值升级（Lv1, Lv2, Lv3…）
- 升级奖励（可预留，如：解锁新皮肤/补签卡）

V1 可先只显示“当前等级”和“距离下一级经验值”，不做太复杂奖励机制。

#### 3.6.3 分享与社交（V1 简单）

- 打卡或查看统计时，可以生成一张分享海报：
  - 显示：
    - 用户昵称（可匿名）
    - 连续打卡天数
    - 本周完成率
  - 海报可转发给好友/保存到相册
- 暂不做公开排行榜和组队打卡，留作后续版本迭代

---

### 3.7 设置与其他模块

#### 3.7.1 个人中心

展示：

- 用户头像 + 昵称
- 会员状态
- 连续打卡天数
- 菜单项：
  - 我的计划（跳转计划设置）
  - 数据统计
  - 会员中心
  - 意见反馈
  - 关于我们 / 服务协议 / 隐私政策

#### 3.7.2 通知与提醒设置

- 在设置中提供：
  - 是否接收打卡提醒
  - 可配置提醒时间（早/晚）
- 实现方式：使用订阅消息（用户手动点击触发订阅）

#### 3.7.3 意见反馈

- 简单表单：
  - 反馈内容（多行文本）
  - 联系方式（选填：微信号/邮箱）
- 提交到后端存库，后续可接入邮箱/企业微信通知

---

## 四、前端设计与交互规范（原生小程序）

### 4.1 技术选型

- 原生微信小程序：
  - 页面：WXML
  - 样式：WXSS
  - 逻辑：JS
  - 配置：JSON
- 使用微信原生组件：
  - `view`, `scroll-view`, `button`, `form`, `input`, `picker`, `switch`, `checkbox`, `radio`, `canvas` 等
- 状态管理：V1 可使用全局 `app.js` + `globalData` 以及本地 `storage` 辅助

### 4.2 页面结构建议

#### 4.2.1 页面列表（`app.json` 中）

```json
{
  "pages": [
    "pages/index/index",          // 首页-今日看板
    "pages/plan/index",           // 计划总览
    "pages/plan/plan-detail",     // 单个维度计划设置
    "pages/record/day-detail",    // 某日打卡详情
    "pages/statistics/index",     // 数据统计
    "pages/vip/index",            // 会员中心
    "pages/user/index",           // 个人中心
    "pages/feedback/index",       // 意见反馈
    "pages/about/index"           // 关于/协议
  ],
  "window": {
    "navigationBarTitleText": "自律教练",
    "navigationBarBackgroundColor": "#ffffff",
    "backgroundColor": "#f7f8fa"
  },
  "tabBar": {
    "color": "#999999",
    "selectedColor": "#07c160",
    "backgroundColor": "#ffffff",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "今日",
        "iconPath": "assets/tab-home.png",
        "selectedIconPath": "assets/tab-home-active.png"
      },
      {
        "pagePath": "pages/statistics/index",
        "text": "数据",
        "iconPath": "assets/tab-stat.png",
        "selectedIconPath": "assets/tab-stat-active.png"
      },
      {
        "pagePath": "pages/user/index",
        "text": "我的",
        "iconPath": "assets/tab-user.png",
        "selectedIconPath": "assets/tab-user-active.png"
      }
    ]
  }
}
```

### 4.3 关键页面交互描述

#### 4.3.1 首页（今日看板）

- 顶部区域：
  - 显示日期 + 小教练头像 + 今日问候语
- 中部任务区域：
  - 按维度分块：
    - 每块卡片显示维度图标、名称、今日完成数量/总数
    - 展开后显示具体任务列表：
      - 任务标题
      - 目标值（比如 30 分钟）
      - 状态（未完成/已完成）
      - 右侧“打卡”按钮
- 底部：
  - 今日总体进度条（已完成任务数 / 总任务数）
  - “查看数据”按钮

交互细节：

- 点击“打卡”：
  - 弹出底部弹层，展示：
    - 任务名称
    - 完成时间/数量输入（如适用）
    - 备注（可选）
    - 确认按钮
  - 确认后：
    - 关闭弹层
    - 更新列表状态
    - 顶部用 `showToast` 或自定义弹窗展示鼓励语

#### 4.3.2 计划设置页

- 列表显示 5 大维度：
  - 每项显示名称 + 开启/关闭 `switch`
  - 右侧箭头进入具体维度设置页
- 新建/编辑计划：
  - 使用表单布局（`form` + `input` + `picker` + `switch` 等）
  - 保存时校验必填项（目标类型、数值等）

---

## 五、数据结构与后端接口（概要）

> 这里用概念模型描述，具体表结构可由后端根据实际数据库调整。

### 5.1 用户表（User）

- `user_id`（openid）
- `nickname`
- `avatar_url`
- `gender`
- `created_at`
- `last_login_at`
- `is_member`
- `member_expire_at`
- `settings`（JSON：提醒偏好等）

### 5.2 计划表（Plan）

- `plan_id`
- `user_id`
- `category`（运动/饮食/睡眠/阅读/学习/custom）
- `title`
- `type`（time/step/count/boolean/custom 等）
- `target_value`（如 30 分钟、8000 步）
- `unit`（分钟、步、页、次 等）
- `week_days`（数组，如 [1,2,3,4,5]）
- `remind_time`（如 "20:00"）
- `is_active`
- `created_at`
- `updated_at`

### 5.3 打卡记录表（Record）

- `record_id`
- `user_id`
- `plan_id`
- `date`（YYYY-MM-DD）
- `value`（实际完成值）
- `status`（完成/未完成/部分）
- `remark`
- `created_at`

### 5.4 会员订单表（Order）

- `order_id`
- `user_id`
- `order_type`（month/quarter/year）
- `amount`
- `pay_status`
- `wechat_pay_trade_no`
- `start_at`
- `end_at`
- `created_at`

---

## 六、非功能性需求

### 6.1 性能

- 首页加载时间：在正常网络下首屏加载 ≤ 2 秒
- 数据请求应尽量合并，减少网络请求次数

### 6.2 安全与隐私

- 遵守微信平台用户隐私保护要求
- 存储用户行为数据（计划、打卡等）时，确保：
  - 不在前端暴露敏感接口地址和密钥
  - 尽量不存储过多个人识别信息

### 6.3 兼容性

- 适配主流微信版本与常见安卓、iOS 机型
- 适配深色模式可作为后续优化项

---

## 七、版本规划（V1）

### 7.1 V1 范围（MVP）

必须上线的功能：

1. 用户基础登录
2. 五大维度的计划设置（每维度至少 1 条计划）
3. 今日任务列表 + 打卡
4. 基础周/月数据统计
5. 会员购买与基础权益（多计划数+补签卡数量）
6. 小教练基础激励语
7. 意见反馈与关于页

### 7.2 后续可以迭代的功能（预留）

- 组队打卡、好友监督
- 内容系统（文章/课程）
- 更复杂的等级、勋章系统
- 与硬件设备/微信运动等更深度的数据联动

---

## 八、后续工作建议

1. 根据本需求文档，进一步拆解为：
   - 页面原型图（每个页面的布局草图）
   - 接口文档（具体请求路径、参数、返回格式）
2. 建立简单的 UI 设计规范：
   - 主色（如绿色系 #07c160）
   - 字体大小/间距/圆角等统一标准
3. 制定开发排期：
   - 第 1 周：原型与 UI 定稿
   - 第 2–4 周：前后端开发与联调
   - 第 5 周：测试与灰度发布
   - 第 6 周：正式上线及首批用户拉新

---

> 本文档定位为 V1.0 产品需求/功能说明，已经可以直接作为开发和 UI 设计沟通的基础。如果你需要，我可以下一步帮你把 **每个页面拆成更细的原型说明**（例如 index 页的具体组件结构、字段、示例数据），或者直接给你一份「原生小程序项目的基础目录结构和示例代码」。