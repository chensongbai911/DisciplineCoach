# 🐛 Bug修复测试指南

**修复日期**: 2025-12-23
**问题**:
1. 左滑报错 `TypeError: requestAnimationFrame is not a function`
2. 点击"添加任务"按钮无反应

---

## 🔧 已修复内容

### 1. requestAnimationFrame Polyfill
**文件**: `miniprogram/components/swipe-action/index.js`
**问题原因**: 微信小程序环境不支持浏览器的 `requestAnimationFrame` API
**解决方案**: 添加polyfill使用 `setTimeout` 模拟60fps的动画帧

```javascript
// Polyfill for requestAnimationFrame in WeChat MiniProgram
const requestAnimationFrame = (callback) => {
  return setTimeout(() => {
    callback(Date.now());
  }, 1000 / 60); // 模拟60fps
};

const cancelAnimationFrame = (id) => {
  clearTimeout(id);
};
```

### 2. 添加任务按钮增强
**文件**:
- `miniprogram/pages/plan/index.js` - showDimensionPicker方法
- `miniprogram/behaviors/index-ui-handler.js` - goToPlan方法

**改进内容**:
- ✅ 添加详细调试日志
- ✅ 添加边界条件检查
- ✅ 添加try-catch错误捕获
- ✅ 添加用户友好的错误提示

---

## 📋 测试步骤

### 测试前准备
1. **清除缓存**
   - 打开微信开发者工具
   - 菜单栏 → 工具 → 清缓存 → 全部清除
   - 或使用快捷键: `Ctrl + Shift + Delete`

2. **完全重新编译**
   - 点击顶部"编译"按钮
   - 或使用快捷键: `Ctrl + B`

3. **打开调试控制台**
   - 点击底部"Console"标签
   - 查看日志输出

---

### 测试1: 左滑功能测试

**测试位置**: 首页(index) - 任务列表

**操作步骤**:
1. 进入首页,确保有任务列表
2. 在任意任务卡片上左滑
3. 观察是否顺滑显示"删除"按钮
4. 查看控制台是否有错误

**预期结果**:
- ✅ 左滑动画流畅,无卡顿
- ✅ 删除按钮正常显示
- ✅ 控制台无 `requestAnimationFrame is not a function` 错误
- ✅ 控制台无任何红色错误信息

**如果失败**:
- 截图控制台错误
- 记录具体操作步骤
- 检查是否已清除缓存

---

### 测试2: 首页添加任务按钮

**测试位置**: 首页(index) - 底部蓝色按钮

**操作步骤**:
1. 进入首页
2. 滚动到页面底部
3. 点击蓝色"🔹 添加任务"按钮
4. 观察控制台日志

**预期结果**:
- ✅ 控制台显示: `[UI Handler] ===== 点击添加任务按钮 =====`
- ✅ 控制台显示: `[UI Handler] 跳转成功`
- ✅ 成功跳转到"计划管理"页面
- ✅ 无任何错误提示

**如果失败**:
- 查看控制台是否有日志输出
- 如果无日志,说明按钮事件未触发(可能是z-index或pointer-events问题)
- 如果有日志但未跳转,截图错误信息

---

### 测试3: 计划页添加任务按钮

**测试位置**: 计划管理页(plan) - 底部蓝色按钮

**前置条件**: 至少开启一个维度(运动/饮食/睡眠/阅读/学习)

**操作步骤**:
1. 进入"计划管理"页面
2. 确保至少有一个维度开关是开启状态(右侧开关为蓝色)
3. 滚动到页面底部
4. 点击蓝色"🔹 添加任务"按钮
5. 观察控制台日志

**预期结果**:
- ✅ 控制台显示: `[plan] ===== 点击添加任务按钮 =====`
- ✅ 控制台显示: `[plan] setData回调 - showPicker已更新为: true`
- ✅ 页面底部弹出维度选择器
- ✅ 选择器显示已开启的维度列表
- ✅ 点击维度后跳转到对应的详情页

**边界情况测试**:
1. **未开启任何维度时**:
   - 按钮应该不显示(条件: `wx:if="{{enabledCount > 0}}"`)
   - 如果按钮显示,点击后应提示"请先开启至少一个维度"

2. **只开启一个维度时**:
   - 选择器只显示该维度
   - 点击后正常跳转

3. **开启多个维度时**:
   - 选择器显示所有已开启的维度
   - 每个维度都可以点击跳转

**如果失败**:
- 查看控制台是否有日志
- 检查 `enabledCount` 的值
- 检查 `showPicker` 是否变为 `true`
- 截图控制台和页面状态

---

## 🔍 调试技巧

### 查看关键数据

**在控制台输入以下命令查看状态**:

```javascript
// 查看计划页数据
const pages = getCurrentPages();
const currentPage = pages[pages.length - 1];
console.log('enabledCount:', currentPage.data.enabledCount);
console.log('showPicker:', currentPage.data.showPicker);
console.log('dimensions:', currentPage.data.dimensions);
```

### 常见问题排查

#### 问题1: 按钮点击无反应,控制台无日志
**可能原因**:
- 按钮被其他元素遮挡(z-index问题)
- 按钮的点击区域无效(pointer-events问题)
- 事件绑定失败

**排查方法**:
1. 在微信开发者工具中右键按钮 → "审查元素"
2. 查看按钮的样式:
   - `z-index` 应该较大(如200)
   - `pointer-events` 应该是 `auto` 或未设置
   - `position` 应该是 `fixed` 或 `absolute`
3. 检查是否有遮罩层(mask)覆盖

#### 问题2: 按钮点击有日志,但功能不正常
**可能原因**:
- 数据状态不正确
- 逻辑判断有误
- API调用失败

**排查方法**:
1. 仔细查看控制台的完整日志
2. 检查是否有错误堆栈
3. 检查数据状态是否符合预期

#### 问题3: 左滑仍然报错
**可能原因**:
- 缓存未清除
- 代码未正确编译

**排查方法**:
1. 再次清除缓存
2. 点击"编译" → "清除缓存并重新编译"
3. 检查 `swipe-action/index.js` 文件开头是否有polyfill代码

---

## 📊 测试结果记录

### 测试环境
- [ ] 微信开发者工具版本: __________
- [ ] 基础库版本: __________
- [ ] 测试时间: __________

### 测试结果

| 测试项 | 是否通过 | 备注 |
|--------|---------|------|
| 左滑功能 | ⬜ 通过 / ⬜ 失败 | |
| 首页添加任务按钮 | ⬜ 通过 / ⬜ 失败 | |
| 计划页添加任务按钮 | ⬜ 通过 / ⬜ 失败 | |
| 维度选择器显示 | ⬜ 通过 / ⬜ 失败 | |
| 维度选择后跳转 | ⬜ 通过 / ⬜ 失败 | |

### 问题记录
如有问题,请详细记录:
```
问题描述:

复现步骤:
1.
2.
3.

错误信息:

截图:
```

---

## 🎯 验收标准

**所有测试项通过后,应达到以下标准**:

1. ✅ 左滑任务卡片流畅无卡顿,无控制台错误
2. ✅ 首页"添加任务"按钮点击后正常跳转到计划页
3. ✅ 计划页"添加任务"按钮点击后弹出维度选择器
4. ✅ 维度选择器显示正确的已开启维度列表
5. ✅ 选择维度后成功跳转到对应维度的详情页
6. ✅ 所有操作都有适当的震动反馈
7. ✅ 所有异常情况都有友好的错误提示
8. ✅ 控制台无红色错误信息

---

## 📝 后续优化建议

如果测试全部通过,可以考虑以下优化:

1. **性能优化**
   - 左滑动画可以进一步优化,使用 `wx.createAnimation` 代替 RAF
   - 按钮防抖,避免快速连续点击

2. **用户体验优化**
   - 添加按钮加载状态(loading)
   - 优化维度选择器动画效果
   - 添加引导提示(首次使用时)

3. **代码质量优化**
   - 抽取公共的日志工具函数
   - 统一错误处理机制
   - 添加单元测试

---

**测试完成后请更新此文档,记录测试结果和遇到的问题!**
